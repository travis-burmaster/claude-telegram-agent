[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "claude-telegram"
version = "0.1.0"
description = "Spin up Claude Code with the official Telegram plugin in one command"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Travis Burmaster", email = "travis@burmaster.com" }]
keywords = ["claude", "claude-code", "telegram", "mcp", "ai", "assistant"]

dependencies = [
    "python-dotenv>=1.0.0",
    "click>=8.1.0",
    "rich>=13.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
claude-telegram = "claude_telegram.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.4.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.4.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.hatch.build.targets.wheel]
packages = ["src/claude_telegram"]
