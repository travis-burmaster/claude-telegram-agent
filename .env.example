# Claude Agent OS environment variables

# Telegram bot token (from @BotFather) -- overrides config.yaml
TELEGRAM_BOT_TOKEN=

# Anthropic API key (Claude will prompt if missing)
ANTHROPIC_API_KEY=

# Optional: override the default model
CLAUDE_MODEL=

# Optional: set Claude's working directory
CLAUDE_WORKSPACE=
